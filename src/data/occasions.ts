/** Seed occasion taxonomy, shared by the Style Book filter row and the
 *  Outfit Builder's occasion picker. Style Book's filter row extends this
 *  with any additional distinct `occasion` values already present on the
 *  user's looks (see StyleBookPage). */
export const seedOccasions: string[] = ["Casual", "Work", "Date Night"];
